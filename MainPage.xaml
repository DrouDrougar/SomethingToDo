<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SomethingToDoApp.MainPage"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Need Something To Do?"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button 
                x:Name="YesButton"
                Text="Yes Give Me Something To Do!"
                SemanticProperties.Hint="Gives you a random task from the list of tasks"
                Clicked="YesButton_OnClicked"
                />
            <Button
                x:Name="AddButton"
                Text="Add a task"
                SemanticProperties.Hint="Takes you to a window where you can add task"
                Clicked="AddButton_OnClicked"
                ></Button>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
